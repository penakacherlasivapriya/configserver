spring:
  application:
    name: business-client
  cloud:
    config:
      uri: http://localhost:8001
      enabled: true

merin:
  message: "Welcome to the Business Client Dev environment!"

server:
  port: 8787

# Config for specific business client services
business-client:
  feature-toggle:
    enabled: true
  custom-property:
    name: "Development Environment Property"